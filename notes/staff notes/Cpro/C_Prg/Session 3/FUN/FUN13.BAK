/*REGISTER VARIABLES:
	  Register is a keyword. It will be acessed only inside of the
function.It will be stored in the machines register.Register variable
can have fast access than the ordinary memory variable.

#include<stdio.h>
#include<conio.h>

/*register int a=30,b=20,c,d;*/
main()
{
  register int a=30,b=20,c,d;
  int add(int,int);
  int sub(int,int);
  clrscr();
  d=add(a,b);
  c=sub(a,b);
  printf("ADDTION=%d\n",d);
  printf("SUBTRACTION=%d\n",c);
  getch();
}
int add(int a,int b)
{
 return(a+b);
}
int sub(int a,int b)
{
 return(a-b);
}

